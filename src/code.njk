---
layout: base
pagination:
  data: codes
  size: 1
  alias: code
permalink: "{{ code.code }}/index.html"
title: HTTP Status Codes
---

<div class="columns is-centered is-gapless">
  <div class="column is-8">
    <header>
      <h1 class="title">Code {{ code.code }}</h1>
      <p class="subtitle has-text-grey">{{ code.title }}{% if code.rfc %} - <a href="{{ code.rfc.link }}" target="_blank">{{ code.rfc.number }}</a>{% endif %}</p>
    </header>
  </div>
</div>

<div class="columns is-centered is-gapless">
  <div class="column is-8">
    <div class="content is-medium">
      <p>{{ code.description | escape }}</p>
      {% if code.refs %}
      <h3 class="title">References</h3>
      <ul>
        {% for ref in code.refs %}
          <li><a href="{{ ref }}">{{ ref }}</a></li>
        {% endfor %}
      </ul>
      {% endif %}
    </div>
  </div>
</div>
