---
layout: base
title: HTTP Status Codes
templateEngineOverride: njk
---

<div class="columns is-centered is-gapless">
  <div class="column is-8">
    <header>
      <h1 class="title">Code {{ code.code }}</h1>
      <p class="subtitle has-text-grey is-4">{{ code.title }}{% if code.tag %}</a> <span class="tag is-light">{{ code.tag }}{% endif %}{% if code.rfc %} - <a href="{{ code.rfc.link }}" target="_blank">{{ code.rfc.number }}</a>{% endif %}</p>
    </header>
  </div>
</div>

<div class="columns is-centered is-gapless">
  <div class="column is-8">
    <div>
      <div class="content is-medium">
        {{ content | safe }}
      </div>
      {% if code.diagram %}
      <div class="mermaid">
        {{ code.diagram }}
      </div>
      {% endif %}
      {% if code.refs %}
      <div class="content is-medium">
        <h3 class="title is-4">References</h3>
        <ul class="ref-list">
          {% for ref in code.refs %}
            <li><a href="{{ ref }}">{{ ref }}</a></li>
          {% endfor %}
        </ul>
      </div>
      {% endif %}
    </div>
    <hr>
    <nav class="breadcrumb is-centered has-succeeds-separator" aria-label="breadcrumbs">
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/#{{ code.group }}">{{ code.group }}</a></li>
        <li class="is-active"><a href="#" aria-current="page">Code {{ code.code }}</a></li>
      </ul>
    </nav>
  </div>
</div>
