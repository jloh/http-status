---
layout: base
title: HTTP Status Codes
templateEngineOverride: njk
---

<div class="py-12 md:pt-12 md:pb-6">
  <div class="container md:max-w-4xl px-4 md:px-0">
    <h1 class="font-bold text-3xl">Code {{ code.code }}</h1>
    <p class="font-normal text-2xl text-gray-600">{{ code.title }}{% if code.tag %}</a> <span class="tag is-light">{{ code.tag }}{% endif %}{% if code.rfc %} - <a href="{{ code.rfc.link }}" target="_blank" class="text-blue-600 hover:text-blue-400">{{ code.rfc.number }}</a>{% endif %}</p>
  </div>
</div>

<div class="flex">
  <div class="container md:max-w-4xl px-4 md:px-0">
    <div class="text-xl font-light border-0 content">
      {{ content | safe }}
    </div>
    {% if code.diagram %}
    <div class="mermaid">
      {{ code.diagram }}
    </div>
    {% endif %}
    {% if code.refs %}
    <div class="content is-medium">
      <h3 class="title is-4">References</h3>
      <ul class="ref-list">
        {% for ref in code.refs %}
          <li><a href="{{ ref }}">{{ ref }}</a></li>
        {% endfor %}
      </ul>
    </div>
    {% endif %}
    <hr class="pb-6 mt-6">
    <nav aria-label="breadcrumbs">
      <ol class="list-reset flex text-lg text-grey-dark justify-center">
        <li class="text-blue-600 hover:text-blue-400"><a href="/">Home</a></li>
        <li><span class="mx-2">/</span></li>
        <li class="text-blue-600 hover:text-blue-400"><a href="/#{{ code.group }}">{{ code.group }}</a></li>
        <li><span class="mx-2">/</span></li>
        <li class="is-active"><a href="#" aria-current="page">Code {{ code.code }}</a></li>
      </ol>
    </nav>
  </div>
</div>
